#include <stdio.h>
int main() 
{
    int a,b,temp;
    printf("enter the two values to be swapped \n");
    scanf("%d%d",&a,&b);
    temp=a ;
    a=b;
    b=temp;
    printf("swapped numbers are =%d%d\n",a,b);
  return 0;
}